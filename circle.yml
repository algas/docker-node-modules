machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
    - "~/node_cache"

  override:
    - if [[ ! -d ~/docker ]]; then mkdir -p ~/docker; fi
    - if [[ ! -d ~/node_cache ]]; then mkdir -p ~/node_cache; fi
    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
    - diff ~/node/package.json ~/node_cache/package.json || docker build --rm=false -t algas/docker-node-modules:build-test1 .
    - docker save algas/docker-node-modules:build-test1 > ~/docker/image.tar

deployment:
  hub:
    branch: test1
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push algas/docker-node-modules:build-test1
